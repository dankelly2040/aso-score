{
  "name": "@aso/core",
  "version": "0.0.0",
  "private": true,
  "description": "Shared ASO grading logic (scoring, categories, URL parsing, App Store types). Consumed as TypeScript source — no build step. Safe for both Expo Metro and Vite.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "sideEffects": false,
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^2.1.8"
  }
}
