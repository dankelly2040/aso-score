# React Native / Expo Metro relies on transitive peers being visible in a flat
# node_modules layout. pnpm's default isolated (symlinked) layout breaks this.
# See: https://docs.expo.dev/guides/monorepos/#pnpm-workspaces
node-linker=hoisted
